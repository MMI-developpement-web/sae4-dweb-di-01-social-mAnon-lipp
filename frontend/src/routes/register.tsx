import RegisterForm from "../components/RegisterForm";

export default function Register() {
  return (
    <main className="bg-auth-gradient flex min-h-screen items-center justify-center p-4 md:p-8">
      <RegisterForm />
    </main>
  );
}
